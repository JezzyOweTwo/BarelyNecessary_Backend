APP_HOST=app                    # when we deploy to AWS, this line needs to change.
APP_PORT=3000
DB_HOST=db                      # when we deploy to AWS, this line needs to change.
DB_PORT=3306
REDIS_HOST=redis                # when we deploy to AWS, this line needs to change.
REDIS_PORT=6379